spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:../picasso/dbms/pcsdb;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL;NON_KEYWORDS=KEY,LEVEL,VALUE
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: picasso-crm-api
